import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginHorizontal: 24
    },
    text: {
        fontSize: 20,
        color: '#fff',
        fontWeight: 'bold',
        marginBottom: 16
    }
})